int missingNumber(vector<int>& a, int n) {
    int total_sum = n * (n + 1) / 2;
    int array_sum = 0;
    
    for (int i = 0; i < n - 1; i++) {
        array_sum += a[i];
    }
    
    return total_sum - array_sum;
}
